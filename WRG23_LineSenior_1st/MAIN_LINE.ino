void LineMainPart() {
  // if (ADD_0.read(0) < ref[0] && ADD_0.read(7) < ref[7]) {
    // int duration = 50;

    // lastReset = millis();
    // while (millis() - lastReset < duration) {
    //   pid(25, 0.01, 0.001);
    // }

    // TestconditionGreen();
    // if (stateTurn == 0) {
    //   L();
    // } else if (stateTurn == 1) {
    //   R();
    // } else if (stateTurn == 3) {
    //   U();
    // }

    // AO();
    // beep();
    // delay(200);
    // if (stateTurn == 2) {
    //   oled.clear();
    // }
    // oled.clear();
    // numberToStringStateTurn();
    // oled.show();
    // waitSW_OK_bmp();
    // delay(500);
  // } else {
    // conditionGreen();
    // pid(35, 0.025, 0.001);
    // pid(30, 0.025, 0.001);

    // pid(25, 0.01, 0.001);
pid(45, 0.025, 0.1);
  // }
}